\* NamespaceDeletion TLC Configuration
\*
\* Run with:
\*   java -jar tla2tools.jar -config NamespaceDeletion.cfg NamespaceDeletion.tla
\*
\* Expected: TLC finds a counterexample for QueryNeverSeesPartialState.
\* The trace shows a query reading a manifest that exists while some
\* fragments have already been deleted by the non-atomic namespace deletion.

SPECIFICATION Spec

\* Primary invariant — EXPECTED TO FAIL.
INVARIANT QueryNeverSeesPartialState

\* Secondary invariant — also expected to fail:
\* INVARIANT NamespaceConsistency

\* Type safety — expected to hold:
\* INVARIANT TypeOK
