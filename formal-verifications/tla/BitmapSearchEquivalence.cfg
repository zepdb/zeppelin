\* BitmapSearchEquivalence TLC Configuration
\*
\* Run with:
\*   java -jar tla2tools.jar -config BitmapSearchEquivalence.cfg BitmapSearchEquivalence.tla
\*
\* Expected: All invariants hold (no counterexamples).
\* Verifies that pre-filter search via bitmaps matches post-filter search.

SPECIFICATION Spec

\* Deadlock is expected: the protocol terminates at pc = "done".
CHECK_DEADLOCK FALSE

\* Type checking.
INVARIANT TypeOK

\* Pre-filter and post-filter return same results.
INVARIANT ResultSetEquivalence

\* Results match expected top-k.
INVARIANT ResultMatchesExpected

\* Pre-filter never misses a candidate.
INVARIANT PreFilterNeverMissesCandidate

\* Fallback path produces correct results.
INVARIANT FallbackCorrect
