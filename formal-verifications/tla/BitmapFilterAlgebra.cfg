\* BitmapFilterAlgebra TLC Configuration
\*
\* Run with:
\*   java -jar tla2tools.jar -config BitmapFilterAlgebra.cfg BitmapFilterAlgebra.tla
\*
\* Expected: All invariants hold (no counterexamples).
\* Verifies that bitmap set operations match brute-force filter evaluation.

SPECIFICATION Spec

\* Deadlock is expected: the state machine can terminate at pc = "done".
CHECK_DEADLOCK FALSE

\* Type checking.
INVARIANT TypeOK

\* Bitmap evaluation matches brute-force.
INVARIANT BitmapMatchesBruteForce

\* No false negatives.
INVARIANT NoFalseNegatives

\* No false positives.
INVARIANT NoFalsePositives

\* None is returned only for legitimate reasons.
INVARIANT NoneIsConservative
