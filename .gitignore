# Rust
/target
fuzz/target/
fuzz/Cargo.lock
loom-tests/target/
loom-tests/Cargo.lock
**/*.rs.bk

# Coverage
lcov.info
*.profraw
*.profdata

# Environment
.env
.env.*
!.env.example

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
dist/
build/

# Task tracking (session state)
tasks/

# Design docs (session state)
docs/

# Test artifacts
tests/fixtures/tmp/
*.proptest-regressions

# Cache
/cache/


# Docker volumes
minio_data/

# Benchmarks and deploy (archived to ~/Documents/code/zep-temp/)
benchmarks/
deploy/
demo/
embed_service/

# Local runtime config (use zeppelin.toml.example as template)
zeppelin.toml

# TLA+ model-checker artifacts
formal-verifications/tla2tools.jar
formal-verifications/tla/*.tlc.log
states/
result_*.txt

# Claude Code local settings
.claude/

# Agent session state and local audit notes
.pi-glla/
AUDIT.md

# Local worktrees and session artifacts
.ext-worktrees/
.tokensave/

# Fuzz corpora are run artifacts; the intentional seeds are already tracked
# and tracked files are never ignored.
fuzz/corpus/
